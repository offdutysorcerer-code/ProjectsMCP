# A0 待辦清單

## Execution Runtime / PowerShell 穩定化

- [x] 建立統一的 A0 Execution Runtime，保留 PowerShell 作為主要通用執行工具之一，但避免各工具自行拼接與直接啟動 PowerShell。（2026-08-18：先於 DEV 導入 `ExecutionRuntimeService`，command plugin 已改由共用 Runtime 執行 CMD / PowerShell，8091 整合測試通過。）
- [x] 建立 `PowerShellRunner`：統一處理 `-NoProfile`、`-NonInteractive`、UTF-8、cwd、env、timeout、stdout/stderr、PID / process tree 與終止流程。（2026-08-18：DEV 導入 `PowerShellRunner`，ExecutionRuntime 改為委派；ProcessService 回傳 child PID；10 項單元測試通過，8091 實測 UTF-8、PID、1 秒 timeout 與 process termination 均通過。）
- [x] 建立 `EnvironmentResolver / Executable Registry`：啟動時偵測並固定 Python、uv、git、node、pwsh / powershell 等 executable 的完整路徑，降低 PATH 差異造成的失敗。（2026-08-18：DEV 新增 `ExecutableRegistry`，啟動時凍結 python/uv/git/node/pwsh/powershell/cmd/codex 路徑；Execution Runtime、Git、Codex、LocalAgent 已接入；8091 整合驗證通過。）
- [x] 建立 `CommandNormalizer / PreflightChecker`：處理路徑 quoting、特殊字元、PowerShell 5.1/7 差異、模組/指令存在性、工作目錄與必要環境變數。（2026-08-18：DEV 新增 `CommandPreflightChecker` 並接入 Execution Runtime；可在 process 建立前阻擋非法 cwd、NUL、缺失 executable、PS7-only 語法跑在 Windows PowerShell 5.1、缺失 module path，並對 CMD/PowerShell 語法混用與 quoting 風險給 warning；20 項測試與 8091 整合驗證通過。）
- [x] 建立 `ErrorClassifier / RecoveryEngine`：對已知錯誤進行分類、自動修復與受控重試。（2026-08-18：DEV 新增 `ErrorClassifier` / `RecoveryEngine`；支援 timeout、command-not-found、ExecutionPolicy、path/module missing、success-with-stderr 等分類；安全 preflight 修復可將明確 `cd /d` 轉成 `Set-Location`，command-not-found 僅在 Startup Registry 有固定路徑時最多重試一次；29 項測試與 8091 實機 recovery/timeout 驗證通過。）
- [x] 建立 `KnownIssuesRegistry`，將曾遇過且已驗證的 PowerShell 問題轉成「執行前預防規則」；新規則須經 recovery 成功與 validator 驗證後才能升級為永久規則。（2026-08-18：DEV 新增 versioned `known_issues.json` 與 `KnownIssuesRegistry`；active 規則需同時滿足 recovery_validated / validator_passed，candidate 無法影響執行；已啟用 `cd /d`→`Set-Location` 與 PowerShell known CLI→Startup Registry 完整路徑預防，CMD 完整路徑規則因實機 validator 失敗而排除；36 項測試與 8091 startup/integration 驗證通過。）
- [x] 先納入既有已知問題：ExecutionPolicy、uv/python/git 找不到、PATH 差異、UTF-8/中文亂碼、路徑與 quoting、cwd 不一致、PowerShell 5.1/7 差異、command timeout、殘留/孤兒 process、模組缺失、CMD/PowerShell 語法混用。（2026-08-18：DEV `known_issues.json` 已正式 seed 全部歷史類別，依風險分為 preventive / classify_only；timeout 與孤兒 process 維持 non-retry + process-tree cleanup，模組缺失與 quoting 不做激進自動修復；39 項測試與 8091 `process_tree_terminated=true` 實機驗證通過。）
- [x] 在 Control Center 增加 PowerShell Runtime Health / Execution Runtime 指標：成功率、自動修復次數、失敗分類、被預防的 Known Issues、常見錯誤排行。（2026-08-18：DEV 新增 UI-neutral `ExecutionRuntimeHealthAggregator`，由 retained command tool telemetry 聚合成功率、timeout/process-tree termination、Recovery/retry、Known Issues prevention、shell/classification/rule 排行；`state.json` / `runtime_snapshot()` 共用資料 contract，現有 Control Center 只新增可替換的 Health section，方便未來整體 UI 重構。25 項相關回歸測試、JS syntax 與 8091 telemetry integration 驗證通過。）
- [x] 架構保持可擴充，未來可加入 `BashRunner` / Linux 執行環境，但不以移除 PowerShell 為目標。（2026-08-19：DEV 新增可選 `BashRunner`，Execution Runtime / Preflight / Executable Registry 正式支援 `shell=bash`；Windows 優先辨識可工作的 Git Bash，避免把無 distro 的 WSL `bash.exe` launcher 誤判為可用 runtime。PowerShell / CMD 行為維持不變；40 項相關回歸與 8091 Git Bash 實機驗證通過。）
